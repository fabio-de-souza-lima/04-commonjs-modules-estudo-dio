//export default


//export default async
exports.connectToDatabase = async (dataName) => {
    console.log("se conectando ao banco de dados: " + dataName);
};

exports.disconnectToDatabase = async ( ) => {
    console.log("se desconectando ao banco de dados: " + dataName);
};